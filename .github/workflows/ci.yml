name: CI staging guard

on:
  push:
    branches: [main]
    paths-ignore:
      - ".bootstrap/**"
  pull_request:

permissions:
  contents: read

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Verified Rune source materialization is pending."
